# Build output
public/

# Dependencies
node_modules/

# Tina-generated artifacts
tina/__generated__/

# base.njk has Jinja interpolation inside the inline JSON-LD <script> block.
# Prettier can't parse that as either Jinja or JSON, so we hand-format it.
src/_includes/layouts/base.njk

# sitemap.njk must have <?xml ?> on the line immediately after the
# frontmatter --- delimiter; prettier insists on a blank line between them,
# which produces leading whitespace before the XML declaration.
src/sitemap.njk

# Misc
*.min.js
*.min.css
package-lock.json

# SEO audit report contains illustrative JSON snippets with `...` placeholders
# that prettier can't parse as JSON.
SEO-AUDIT-*.md
